<?php

class MagazineTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('magazine')->truncate();

		$magazine = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('magazine')->insert($magazine);
	}

}
